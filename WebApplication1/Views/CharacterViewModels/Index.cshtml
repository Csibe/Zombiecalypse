@model IEnumerable<WebApplication1.ViewModels.CharacterViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CharacterName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CharacterName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CharacterViewModelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CharacterViewModelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CharacterViewModelID })
        </td>
    </tr>
}

</table>
