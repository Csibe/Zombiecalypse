@model IEnumerable<Zombiecalypse.Models.Inventory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Item.ItemID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Character.CharacterName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Item.ItemName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ItemPieces)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Building.BuildingLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Item.ItemID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Character.CharacterName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Item.ItemName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemPieces)
        </td>
        <th>
            @Html.DisplayFor(modelItem => item.Building.BuildingLevel)
        </th>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InventoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InventoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InventoryID }) |
            @Html.ActionLink("AddToItem", "AddToItem", new { id = item.InventoryID })
            @Html.ActionLink("LevelUpBuilding", "LevelUpBuilding", new { id = item.InventoryID })
        </td>
    </tr>
}

</table>
